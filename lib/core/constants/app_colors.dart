import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const Color primary = Color(0xFFFF8C00); // برتقالي
  static const Color primaryDark = Color(0xFFE67600);

  static const Color background = primary;
  static const Color surface = Colors.white;

  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Color(0xFFFFD600); // أصفر

  static const Color petrolGreen = Color(0xFF0F4C5C);
}
